<div class="col">
    <h1 class="m-0 text-dark">Votre profil</h1>
</div>
<div class="col">
    <ol class="breadcrumb float-sm-right">
        <li class="breadcrumb-item"><a href="{{ path('home') }}">Home</a></li>

        {%  if domaine is defined %}
            <li class="breadcrumb-item"><a href="{{ path('profil_home') }}">Votre profil</a></li>
            <li class="breadcrumb-item active">{{ domaine }}</li>
        {%  else  %}
            <li class="breadcrumb-item active">Votre profil</li>
        {% endif %}
    </ol>
</div>