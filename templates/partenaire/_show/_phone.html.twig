{% if partenaire.phone1 or partenaire.phone1 %}
    <section>
        <strong><i class="fa fa-phone mr-1"></i> Téléphone</strong>
        <p class="text-muted">
            {% if partenaire.phone1 %}
                {{ partenaire.phone1 }}<br/>
            {% endif %}
            {% if partenaire.phone1 %}
                {{ partenaire.phone1 }}
            {% endif %}
        </p>
    </section>
{% endif %}
