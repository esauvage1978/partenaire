<div class="col">
    <h1 class="m-0 text-dark">{{ domaine }} </h1>
</div>
<div class="col">
    <ol class="breadcrumb float-sm-right">
        <li class="breadcrumb-item"><a href="{{ path('home') }}">Home</a></li>
        <li class="breadcrumb-item"><a href="{{ path('admin_home') }}">Administration</a></li>
        {% if id  is defined %}
            <li class="breadcrumb-item"><a href="{{ path(index) }}">Liste</a></li>
            <li class="breadcrumb-item active">{{ domaine }} {{ id }}</li>
        {% elseif index is defined %}
            <li class="breadcrumb-item"><a href="{{ path(index) }}">Liste</a></li>
            <li class="breadcrumb-item active">Création</li>
        {% else %}
            <li class="breadcrumb-item active">Liste</li>
        {% endif %}
    </ol>
</div>